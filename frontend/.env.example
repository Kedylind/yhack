# Copy to frontend/.env for local overrides. Never commit real secrets.

# Production / preview: public API origin (e.g. https://your-api.up.railway.app).
# Local dev: leave empty — Vite proxies /api to http://127.0.0.1:8000 (see vite.config.ts).
VITE_API_BASE_URL=
